#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(a>=0)
    {
        printf("The number is possitive\n");
    }
    else if(a<0)
    {
        printf("The number is negative\n");
    }

    return 0;
}
